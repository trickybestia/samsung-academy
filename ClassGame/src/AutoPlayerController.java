public class AutoPlayerController extends PlayerController {
    @Override
    public int selectAttackTarget(Player player, Player enemy, int attackingUnitIndex) {
        int selectedIndex = 0;

        System.out.println(selectedIndex + 1);

        return selectedIndex;
    }
}
